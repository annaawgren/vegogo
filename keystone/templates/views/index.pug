extends ../layouts/default

block content
	.container: .jumbotron
		h1 Vegogo admin
		if user && user.canAccessKeystone
			p
				a(href='/keystone').btn.btn-lg.btn-primary Gå till admin
		else
			<!-- p We have created a default Admin user for you with the email <strong>par.thernstrom@gmail.com</strong> and the password <strong>mlpoe434A</strong>. -->
			p
				a(href='/keystone/signin', style='margin-right: 10px').btn.btn-lg.btn-primary Logga in
